# Local development overrides
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:

  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: axon/app:local
    volumes:
      - .:/app
      - /app/node_modules    # Keep container node_modules separate from host
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: "true"    # Required for file-watching on Windows and WSL

  # --------------------------------------------------------------------------
  # Local database - uncomment the one that matches your project
  # This is for local dev only - production DB is external (not in compose)
  # --------------------------------------------------------------------------

  # PostgreSQL
  # db:
  #   image: postgres:17-alpine
  #   environment:
  #     POSTGRES_DB: ${DB_DATABASE:-app_dev}
  #     POSTGRES_USER: ${DB_USERNAME:-app}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-app}"]
  #     interval: 5s
  #     retries: 10

  # MySQL
  # db:
  #   image: mysql:8
  #   environment:
  #     MYSQL_DATABASE: ${DB_DATABASE:-app_dev}
  #     MYSQL_USER: ${DB_USERNAME:-app}
  #     MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
  #     MYSQL_ROOT_PASSWORD: rootsecret
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql

  # MongoDB
  # db:
  #   image: mongo:7
  #   environment:
  #     MONGO_INITDB_DATABASE: ${DB_DATABASE:-app_dev}
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo_data:/data/db

# volumes:
#   postgres_data:
#   mysql_data:
#   mongo_data:
