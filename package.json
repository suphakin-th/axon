{
  "name": "axon",
  "version": "1.0.0",
  "description": "Universal CI/CD scaffold for GitHub Actions and GitLab CI",
  "scripts": {
    "test": "echo \"Replace this with your test command (jest, vitest, mocha, etc.)\" && exit 0",
    "dev": "echo \"Replace this with your dev server command (node, nodemon, ts-node, etc.)\" && exit 0",
    "build": "echo \"Replace this with your build command (tsc, vite build, webpack, etc.)\" && exit 0"
  },
  "keywords": ["cicd", "docker", "github-actions", "gitlab-ci", "scaffold"],
  "license": "MIT"
}
